---
import Layout from "../layouts/Layout.astro"; // 🔹 Ruta relativa corregida
const { frontmatter } = Astro.props;
const bookLabel = frontmatter.bookLabel ?? 'Book Now';
const offers = await Astro.glob("../pages/savings/*.md"); // 🔹 Ajuste ruta relativa a savings
const suggestedoffers = offers.filter(
  (room) => room.frontmatter.title !== frontmatter.title
);
---

<Layout title={`${frontmatter.title} Offers - La Casa Que Canta`}>

<section class="">
  {(!frontmatter.hideHero) && (
    <div>
      <img
        class="h-72 w-full object-cover"
        src={frontmatter.imagefront}
        alt=""
      />
    </div>
  )}
  {frontmatter.hideHero ? (
    <>
      <!-- Top white header with title/date only -->
      <div class="bg-white">
        <div class="max-w-7xl mx-auto px-4 py-10">
          <h1 class="text-5xl lg:text-7xl font-bold">{frontmatter.title}</h1>
        </div>
      </div>

      <!-- Full-bleed background image starts BELOW the header -->
      <div class="relative w-full min-h-[90vh] md:min-h-screen">
        <img class="absolute inset-0 w-full h-full object-cover object-[center_25%]" src={frontmatter.imagefront} alt="" />
        <div class="relative z-10 max-w-7xl mx-auto h-full px-4 py-12 md:py-16 flex items-start md:items-center">
        <div class="max-w-xl">
          <div class="relative bg-gradient-to-b from-gray-900/40 to-gray-900/70 backdrop-blur-sm p-6 md:p-8 pb-[2cm] text-sm md:text-base text-white">
            <slot />
            <hr class="border-white/30 my-4" />
            <div class="mt-4">
              <p class="text-[#CE33A1]">
                Direct : 755 555 7000<br />
                USA : 1 888 523 5050<br />
                552 564 1838<br />
                <a class="underline text-[#CE33A1]" href="mailto:sales.reservations@lacasaquecanta.com">sales.reservations@lacasaquecanta.com</a>
              </p>
            </div>
            <div class="mt-6 flex justify-end">
              <a href={frontmatter.book} class="text-sm bg-[#CE33A1] hover:bg-[#d647ad] font-semibold uppercase py-3 px-6 text-white">{bookLabel}</a>
            </div>
          </div>
          </div>
        </div>
      </div>
    </>
  ) : (
    <!-- Default content layout -->
    <div class="max-w-7xl mx-auto py-16 px-4">
      <div class="max-w-xl">
        <div>
          <h1 class="text-5xl lg:text-7xl font-bold">{frontmatter.title}</h1>
          <p class="mb-3">Offer available from <b>{frontmatter.date}</b></p>
        </div>
        <div class="flex gap-4 flex-wrap mb-8">
          <a href={frontmatter.book} class="w-full md:w-auto text-center text-sm bg-black/90 font-semibold uppercase py-3 px-6 text-white hover:bg-black/60">{bookLabel}</a>
        </div>
        <div class="text-sm md:text-base">
          <slot />
          <div class="mt-4">
            <p>
              Direct : 755 555 7000<br />
              USA : 1 888 523 5050<br />
              552 564 1838<br />
              <a class="underline" href="mailto:sales.reservations@lacasaquecanta.com">sales.reservations@lacasaquecanta.com</a>
            </p>
          </div>
        </div>
      </div>
    </div>
  )}

  
</section>
</Layout>
